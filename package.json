{
  "name": "smoke-break-cat",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "check": "node scripts/check-project.mjs",
    "test": "npm run check"
  },
  "devDependencies": {
    "vite": "^8.2.2"
  }
}
